page.replace_html(:tooltip_header, '<b>' + link_to(@connection.first_name + " " + @connection.last_name, @connection.logged_in_url) + '</b><BR>' + @connection.headline)
page.replace_html(:tooltip_body, '<BR><BR>' +
 @connection.industry + '<BR>' + @connection.location)
